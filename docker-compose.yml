services:
  vivid:
    build: .
    container_name: vivid-agent
    ports:
      - "3100:3100"
    environment:
      NVIDIA_API_KEY: ${NVIDIA_API_KEY}
      VIVID_HOME: /data
      PORT: 3100
    volumes:
      - ./data:/data
    restart: unless-stopped